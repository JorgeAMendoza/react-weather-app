// component responsible for each forecast weather, takes in data and just displays it

const Forecast = () => {
  return (
    <div>
      <p>forecast tab</p>
    </div>
  );
};

export default Forecast;
